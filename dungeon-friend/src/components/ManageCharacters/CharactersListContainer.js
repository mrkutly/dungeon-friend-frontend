import React from 'react'

const CharactersListContainer = (props) => {

  // const mappedCharacters = props.characters.map(character => {
  //   return <h1>I'm a character</h1>
  // })

  return (
    <div>
      {/* {mappedCharacters()} */}
      I'm a characters list
    </div>
  )
}

export default CharactersListContainer
